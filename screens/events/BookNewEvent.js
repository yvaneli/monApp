import React from 'react';
import {View, Text} from 'native-base';

export const BookNewEvent = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'white',
      }}>
      <Text>Create New Event Page</Text>
    </View>
  );
};

export default BookNewEvent;
